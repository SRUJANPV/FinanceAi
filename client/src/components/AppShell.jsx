import { LayoutDashboard, ReceiptText, WalletCards, LogOut, Menu, Sparkles, ChartNoAxesCombined, Target, Landmark, ShieldCheck, Moon, Sun } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { useTheme } from '../context/ThemeContext';
import NotificationCenter from './NotificationCenter';
const navigation = [{ to: '/dashboard', label: 'Overview', icon: LayoutDashboard }, { to: '/transactions', label: 'Transactions', icon: ReceiptText }, { to: '/budgets', label: 'Budgets', icon: WalletCards }, { to: '/analytics', label: 'Analytics', icon: ChartNoAxesCombined }, { to: '/wallets', label: 'Accounts', icon: WalletCards }, { to: '/goals', label: 'Goals', icon: Target }, { to: '/loans', label: 'Loans & EMIs', icon: Landmark }];
export default function AppShell() { const { user, logout } = useAuth(); const { theme, toggleTheme } = useTheme(); const links = user?.role === 'admin' ? [...navigation, { to: '/admin', label: 'Admin', icon: ShieldCheck }] : navigation; return <div className="min-h-screen bg-slate-50 text-slate-900 dark:bg-slate-950 dark:text-slate-100"><aside className="fixed inset-y-0 hidden w-64 border-r border-slate-200/70 bg-white/75 p-5 backdrop-blur-xl dark:border-white/10 dark:bg-slate-900/75 lg:block"><NavLink to="/dashboard" className="mb-10 flex items-center gap-2 text-lg font-bold"><span className="grid h-8 w-8 place-items-center rounded-xl bg-brand-500 text-white"><Sparkles size={16}/></span>SmartSpend</NavLink><nav className="space-y-1">{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} className={({isActive}) => `flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium ${isActive ? 'bg-brand-500 text-white shadow-glow' : 'text-slate-500 hover:bg-slate-100 dark:hover:bg-white/5'}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="absolute bottom-5 left-5 right-5"><p className="mb-3 truncate text-sm font-medium">{user?.name}</p><button onClick={logout} className="flex items-center gap-2 text-sm text-slate-500 hover:text-rose-500"><LogOut size={16}/>Sign out</button></div></aside><main className="lg:ml-64"><header className="flex h-16 items-center justify-between border-b border-slate-200/70 bg-white/50 px-5 backdrop-blur dark:border-white/10 dark:bg-slate-900/50"><Menu className="lg:hidden"/><div className="ml-auto flex items-center gap-1"><button onClick={toggleTheme} aria-label="Toggle color theme" className="grid h-9 w-9 place-items-center rounded-lg text-slate-500 hover:bg-slate-100 dark:hover:bg-white/10">{theme === 'dark' ? <Sun size={18}/> : <Moon size={18}/>}</button><NotificationCenter/><p className="hidden rounded-full bg-brand-50 px-3 py-1 text-xs font-medium text-brand-600 dark:bg-brand-500/10 sm:block">AI finance, made clear</p></div></header><div className="mx-auto max-w-7xl p-5 sm:p-8"><Outlet/></div></main></div>; }
