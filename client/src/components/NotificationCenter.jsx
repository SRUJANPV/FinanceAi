import { useState } from 'react'; import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'; import { Bell, CheckCheck, X } from 'lucide-react'; import { api } from '../lib/api';
export default function NotificationCenter() { const [open, setOpen] = useState(false), client = useQueryClient(); const { data } = useQuery({ queryKey: ['notifications'], queryFn: () => api.get('/notifications').then((r) => r.data.data), refetchInterval: 60_000 }); const markOne = useMutation({ mutationFn: (id) => api.patch(`/notifications/${id}/read`), onSuccess: () => client.invalidateQueries({ queryKey: ['notifications'] }) }); const markAll = useMutation({ mutationFn: () => api.patch('/notifications/read-all'), onSuccess: () => client.invalidateQueries({ queryKey: ['notifications'] }) }); const unread = data?.unread || 0; return <div className="relative"><button onClick={() => setOpen(!open)} aria-label="Notifications" className="relative grid h-9 w-9 place-items-center rounded-lg text-slate-500 hover:bg-slate-100 dark:hover:bg-white/10"><Bell size={19}/>{unread > 0 && <span className="absolute right-1 top-1 h-2 w-2 rounded-full bg-rose-500"/>}</button>{open && <div className="absolute right-0 z-50 mt-2 w-[min(24rem,calc(100vw-2rem))] overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl dark:border-white/10 dark:bg-slate-900"><div className="flex items-center justify-between border-b border-slate-100 p-4 dark:border-white/10"><div><h2 className="font-semibold">Notifications</h2><p className="text-xs text-slate-500">{unread ? `${unread} unread` : 'You’re all caught up'}</p></div><div className="flex gap-1"><button title="Mark all as read" onClick={() => markAll.mutate()} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-white/10"><CheckCheck size={17}/></button><button title="Close" onClick={() => setOpen(false)} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-white/10"><X size={17}/></button></div></div><div className="max-h-96 overflow-y-auto">{data?.notifications?.length ? data.notifications.map((item) => <button key={item._id} onClick={() => !item.readAt && markOne.mutate(item._id)} className={`block w-full border-b border-slate-100 p-4 text-left last:border-0 dark:border-white/5 ${item.readAt ? 'opacity-60' : 'bg-brand-50/50 dark:bg-brand-500/5'}`}><p className="text-sm font-semibold">{item.title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{item.message}</p><p className="mt-2 text-[11px] text-slate-400">{new Date(item.createdAt).toLocaleString()}</p></button>) : <p className="p-8 text-center text-sm text-slate-500">No notifications yet.</p>}</div></div>}</div>; }
