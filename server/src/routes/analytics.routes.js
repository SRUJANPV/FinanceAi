import { Router } from 'express'; import { protect } from '../middlewares/auth.middleware.js'; import { overview, details } from '../controllers/analytics.controller.js'; const router = Router(); router.get('/overview', protect, overview); router.get('/details', protect, details); export default router;
